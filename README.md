This website is used to practise basic HTML and CSS. 

It includes a navigation bar, with four pages (Home, About, Contact, Location).

The Contact page includes a form with some validation.

The Location page includes an iframe to display the location map, with two JavaScript functions (Reload Map and View Fullscreen).
